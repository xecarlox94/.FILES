#!/bin/sh


alias gst="git status"
alias gp="git push"


alias sd="sudo shutdown now"
alias rb="reboot"

alias tf="terraform"

alias py="python3"

alias :q="exit"

alias new_shell="clear && $SHELL && exit"
